# Modify this for your deployment

# Service hostname (zimbraServiceHostname)
SERVICE_HOSTNAME=mail.example.test

# Default domain (zimbraDefaultDomainName)
# If set to root domain such as example.test, you need to ensure DNS has A and MX setup correctly.
DEFAULT_DOMAIN=mail.example.test

# Use different admin login than the usual `admin`.
# The password will be set to Must Change when first login.
ADMIN_USERNAME=mailadmin
ADMIN_PASSWORD=zimbra

# Local timezone
TIME_ZONE=Asia/Kuala_Lumpur

# Refer to zm-images for list of images and version to use
ZIMBRA_IMAGE=yeak/zimbra-aio
VERSION=10.1.15.p1

# Use custom network so that MTA mynetworks is static
DOCKER_SUBNET=172.28.0.0/24

# Juicefs config
# Note: volume name *cannot* contain "_" or "."
#VOLUME_PREFIX=mail-example-test
#BUCKET=https://YOUR_S3_BUCKET_URL
#ACCESS_KEY=YOUR_S3_ACCESS_KEY
#SECRET_KEY=YOUR_S3_SECRET_KEY
